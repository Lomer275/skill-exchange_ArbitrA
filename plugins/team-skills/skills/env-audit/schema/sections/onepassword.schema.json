{
  "type": ["object", "null"],
  "additionalProperties": false,
  "required": [
    "op_path", "op_version", "signed_in", "signed_in_reason",
    "vaults_count", "team_vault_present", "team_vault_source",
    "service_account_env", "env_files"
  ],
  "properties": {
    "op_path": {"type": ["string", "null"]},
    "op_version": {"type": ["string", "null"]},
    "signed_in": {"type": ["boolean", "null"]},
    "signed_in_reason": {"enum": ["ok", "not_installed", "not_confirmed", "timeout"]},
    "vaults_count": {"type": ["integer", "null"], "minimum": 0},
    "team_vault_present": {"type": ["boolean", "null"]},
    "team_vault_source": {"enum": ["definitions", "none"]},
    "service_account_env": {"type": "boolean"},
    "env_files": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "total", "templates", "with_values", "op_refs_only", "mixed",
        "mode_wider_than_600_with_values", "by_root"
      ],
      "properties": {
        "total": {"type": "integer", "minimum": 0},
        "templates": {"type": "integer", "minimum": 0},
        "with_values": {"type": "integer", "minimum": 0},
        "op_refs_only": {"type": "integer", "minimum": 0},
        "mixed": {"type": "integer", "minimum": 0},
        "mode_wider_than_600_with_values": {"type": "integer", "minimum": 0},
        "by_root": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "additionalProperties": false,
            "required": ["total", "with_values", "op_refs_only", "mixed"],
            "properties": {
              "total": {"type": "integer", "minimum": 0},
              "with_values": {"type": "integer", "minimum": 0},
              "op_refs_only": {"type": "integer", "minimum": 0},
              "mixed": {"type": "integer", "minimum": 0}
            }
          }
        }
      }
    }
  }
}
